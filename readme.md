## Cluster / Express NodeJS test

To run it with the cluster active/inactive just run the command:

`node server [true/false]`

#### Before you run it, make sure to install the dependencies with

`npm install`

##### Have fun!
